{
  "name": "manito-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "nodemon --exec ./bin/www",
    "deploy-deprecated": "pm2 delete manito-server || : && cross-env NODE_ENV=production pm2 start ./bin/www --name manito-server",
    "deploy": "cross-env NODE_ENV=production firebase deploy --only functions"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^7.28.0"
  },
  "devDependencies": {
    "eslint-config-prettier": "^8.3.0"
  }
}
